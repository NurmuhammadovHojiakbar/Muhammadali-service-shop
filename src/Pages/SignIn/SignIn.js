import { Box } from "@mui/material"

const SignIn = () => {

    const loginStyles = {
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        height: "100vh",
    }

    return (
        <Box component="div" sx={loginStyles}>
        </Box>
    );
}
 
export default SignIn;